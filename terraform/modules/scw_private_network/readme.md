# Scaleway Private Network

Create Private Networks for your resources, allowing them to communicate on a secure network without needing a public IP address.

Note:

- Max network size is /20 (4094 addresses)
- Max 8 Private networks per VPC
- Max 512 resources per Private network
